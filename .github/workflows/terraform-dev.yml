---
name: Terraform (Dev)

on:
  pull_request:
    branches:
      - main
    paths:
      - "infra/terraform/envs/dev/**"
      - "infra/terraform/modules/**"
      - ".github/workflows/terraform-dev.yml"
  workflow_dispatch:
    inputs:
      RUN_APPLY:
        description: "Apply実行フラグ（falseの場合はplanのみ）"
        required: true
        type: boolean
        default: false
  workflow_call:
    inputs:
      RUN_APPLY:
        description: "Apply実行フラグ（falseの場合はplanのみ）"
        required: false
        type: boolean
        default: true

concurrency:
  group: terraform-dev-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_ACCOUNT_ID: "072693953877"
  AWS_REGION: ap-northeast-1
  OIDC_IAM_ROLE: prod-blog-deploy-role
  RUN_APPLY: ${{ inputs.RUN_APPLY || 'false' }}

jobs:
  # CloudFlare (cms-api) - Dev
  terraform-cloudflare-cms-api-dev:
    name: CloudFlare (cms-api-dev)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      deployments: write
      pull-requests: write
    timeout-minutes: 20
    env:
      WORKING_DIRECTORY: infra/terraform/envs/dev/cms-api

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Terraform Environment
        uses: ./.github/actions/setup-terraform-env
        with:
          aws-account-id: ${{ env.AWS_ACCOUNT_ID }}
          aws-region: ${{ env.AWS_REGION }}
          oidc-iam-role: ${{ env.OIDC_IAM_ROLE }}
          working-directory: ${{ env.WORKING_DIRECTORY }}

      - name: Terraform Plan
        id: plan
        continue-on-error: true
        working-directory: ${{ env.WORKING_DIRECTORY }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          TF_VAR_cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          terraform plan -detailed-exitcode -no-color 2>&1 | tee tf_plan.txt

      - name: Set Plan Status
        id: plan-status
        run: |
          case "${{ steps.plan.outputs.exitcode }}" in
            0) echo "status=no-changes" >> $GITHUB_OUTPUT ;;
            2) echo "status=has-diff" >> $GITHUB_OUTPUT ;;
            *) echo "status=error" >> $GITHUB_OUTPUT ;;
          esac

      - name: tfcmt Plan Comment
        if: github.event_name == 'pull_request' && steps.plan.outputs.exitcode != '1'
        working-directory: ${{ env.WORKING_DIRECTORY }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TARGET=$(echo "${{ env.WORKING_DIRECTORY }}" | sed -e 's|^.*infra/terraform/envs/||' | cut -c 1-36)
          tfcmt --config $GITHUB_WORKSPACE/.github/tfcmt.yml --var target:$TARGET plan -patch -- cat tf_plan.txt

      - name: Discord Notification (Plan Error)
        if: steps.plan.outputs.exitcode == '1'
        continue-on-error: true
        uses: ./.github/actions/discord-notify
        with:
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_DEV }}
          title: "Terraform Plan Error: cms-api-dev"
          message: "path: `${{ env.WORKING_DIRECTORY }}`"

      - name: Fail on Plan Error
        if: steps.plan.outputs.exitcode == '1'
        run: |
          echo "::error::Terraform plan failed with errors"
          exit 1

      - name: Start Deployment
        if: steps.plan-status.outputs.status == 'has-diff' && github.ref == 'refs/heads/main'
        uses: bobheadxi/deployments@v1
        id: deployment
        with:
          step: start
          token: ${{ secrets.GITHUB_TOKEN }}
          env: development-cloudflare

      - name: Terraform Apply
        if: |
          steps.plan-status.outputs.status == 'has-diff' &&
          env.RUN_APPLY == 'true'
        working-directory: ${{ env.WORKING_DIRECTORY }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          TF_VAR_cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: terraform apply -auto-approve

      - name: Finish Deployment
        if: steps.plan-status.outputs.status == 'has-diff' && always() && env.RUN_APPLY == 'true'
        uses: bobheadxi/deployments@v1
        with:
          step: finish
          token: ${{ secrets.GITHUB_TOKEN }}
          status: ${{ job.status }}
          env: ${{ steps.deployment.outputs.env }}
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}

  # Vercel (frontend) - Dev
  terraform-vercel-dev:
    name: Vercel (frontend-dev)
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      deployments: write
      pull-requests: write
    timeout-minutes: 20
    env:
      WORKING_DIRECTORY: infra/terraform/envs/dev/frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Terraform Environment
        uses: ./.github/actions/setup-terraform-env
        with:
          aws-account-id: ${{ env.AWS_ACCOUNT_ID }}
          aws-region: ${{ env.AWS_REGION }}
          oidc-iam-role: ${{ env.OIDC_IAM_ROLE }}
          working-directory: ${{ env.WORKING_DIRECTORY }}

      - name: Terraform Plan
        id: plan
        continue-on-error: true
        working-directory: ${{ env.WORKING_DIRECTORY }}
        env:
          VERCEL_API_TOKEN: ${{ secrets.VERCEL_API_TOKEN }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_vercel_api_token: ${{ secrets.VERCEL_API_TOKEN }}
          TF_VAR_cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_cms_api_key: ${{ secrets.CMS_API_KEY_DEV }}
          TF_VAR_basic_auth_user: ${{ secrets.BASIC_AUTH_USER }}
          TF_VAR_basic_auth_pass: ${{ secrets.BASIC_AUTH_PASS }}
          TF_VAR_admin_password_hash: ${{ secrets.ADMIN_PASSWORD_HASH_DEV }}
          TF_VAR_auth_secret: ${{ secrets.AUTH_SECRET_DEV }}
        run: |
          terraform plan -detailed-exitcode -no-color 2>&1 | tee tf_plan.txt

      - name: Set Plan Status
        id: plan-status
        run: |
          case "${{ steps.plan.outputs.exitcode }}" in
            0) echo "status=no-changes" >> $GITHUB_OUTPUT ;;
            2) echo "status=has-diff" >> $GITHUB_OUTPUT ;;
            *) echo "status=error" >> $GITHUB_OUTPUT ;;
          esac

      - name: tfcmt Plan Comment
        if: github.event_name == 'pull_request' && steps.plan.outputs.exitcode != '1'
        working-directory: ${{ env.WORKING_DIRECTORY }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TARGET=$(echo "${{ env.WORKING_DIRECTORY }}" | sed -e 's|^.*infra/terraform/envs/||' | cut -c 1-36)
          tfcmt --config $GITHUB_WORKSPACE/.github/tfcmt.yml --var target:$TARGET plan -patch -- cat tf_plan.txt

      - name: Discord Notification (Plan Error)
        if: steps.plan.outputs.exitcode == '1'
        continue-on-error: true
        uses: ./.github/actions/discord-notify
        with:
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_DEV }}
          title: "Terraform Plan Error: frontend-dev"
          message: "path: `${{ env.WORKING_DIRECTORY }}`"

      - name: Fail on Plan Error
        if: steps.plan.outputs.exitcode == '1'
        run: |
          echo "::error::Terraform plan failed with errors"
          exit 1

      - name: Start Deployment
        if: steps.plan-status.outputs.status == 'has-diff' && github.ref == 'refs/heads/main'
        uses: bobheadxi/deployments@v1
        id: deployment
        with:
          step: start
          token: ${{ secrets.GITHUB_TOKEN }}
          env: development-vercel

      - name: Cleanup CloudFlare DNS
        if: |
          steps.plan-status.outputs.status == 'has-diff' &&
          env.RUN_APPLY == 'true'
        uses: ./.github/actions/cleanup-cloudflare-dns
        with:
          zone-id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          subdomain: blog-dev
          domain: tqer39.dev
          keep-type: CNAME

      - name: Terraform Apply
        if: |
          steps.plan-status.outputs.status == 'has-diff' &&
          env.RUN_APPLY == 'true'
        working-directory: ${{ env.WORKING_DIRECTORY }}
        env:
          VERCEL_API_TOKEN: ${{ secrets.VERCEL_API_TOKEN }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_vercel_api_token: ${{ secrets.VERCEL_API_TOKEN }}
          TF_VAR_cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          TF_VAR_cms_api_key: ${{ secrets.CMS_API_KEY_DEV }}
          TF_VAR_basic_auth_user: ${{ secrets.BASIC_AUTH_USER }}
          TF_VAR_basic_auth_pass: ${{ secrets.BASIC_AUTH_PASS }}
          TF_VAR_admin_password_hash: ${{ secrets.ADMIN_PASSWORD_HASH_DEV }}
          TF_VAR_auth_secret: ${{ secrets.AUTH_SECRET_DEV }}
        run: terraform apply -auto-approve

      - name: Finish Deployment
        if: steps.plan-status.outputs.status == 'has-diff' && always() && env.RUN_APPLY == 'true'
        uses: bobheadxi/deployments@v1
        with:
          step: finish
          token: ${{ secrets.GITHUB_TOKEN }}
          status: ${{ job.status }}
          env: ${{ steps.deployment.outputs.env }}
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}

  # Workflow result check
  workflow-result:
    name: Workflow Result
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs:
      - terraform-cloudflare-cms-api-dev
      - terraform-vercel-dev
    if: always()
    steps:
      - name: Check Results
        run: |
          if [[ "${{ needs.terraform-cloudflare-cms-api-dev.result }}" == "failure" ]] || \
             [[ "${{ needs.terraform-cloudflare-cms-api-dev.result }}" == "cancelled" ]] || \
             [[ "${{ needs.terraform-vercel-dev.result }}" == "failure" ]] || \
             [[ "${{ needs.terraform-vercel-dev.result }}" == "cancelled" ]]; then
            echo "::error::Workflow failed"
            exit 1
          fi
          echo "Workflow completed successfully"
