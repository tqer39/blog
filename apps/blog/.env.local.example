# ===========================================
# CMS API Configuration
# ===========================================
# CMS API endpoint (Required)
# Default: http://localhost:3101/v1
CMS_API_URL=http://localhost:3101/v1

# CMS API authentication key (Required)
# Must match the key configured in apps/cms-api/.dev.vars
CMS_API_KEY=dev-api-key

# ===========================================
# Authentication
# ===========================================
# Session signing secret (Required)
# Generate: openssl rand -base64 32
AUTH_SECRET=

# Admin password hash for /admin login (Required)
# Generate: node -e "require('bcryptjs').hash('your-password', 12).then(console.log)"
ADMIN_PASSWORD_HASH=

# ===========================================
# Site Configuration (Public)
# ===========================================
# Site URL for SEO and metadata (Required)
NEXT_PUBLIC_SITE_URL=http://localhost:3100

# Google Analytics 4 Measurement ID (Optional)
# Format: G-XXXXXXXXXX
# NEXT_PUBLIC_GA_ID=

# ===========================================
# Basic Auth (Development Environment Only)
# ===========================================
# Enable Basic Auth protection for entire site
# Used in proxy.ts for dev environment access control
# BASIC_AUTH_ENABLED=true
# BASIC_AUTH_USER=
# BASIC_AUTH_PASS=
